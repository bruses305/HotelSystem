<Page x:Class="HotelSystem.Views.BookingsView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:System="clr-namespace:System;assembly=mscorlib"
 Background="#ECF0F1">
<Grid Margin="20">
<Grid.RowDefinitions>
<RowDefinition Height="Auto"/>
<RowDefinition Height="*"/>
</Grid.RowDefinitions>

<Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
<Grid>
<Grid.ColumnDefinitions>
<ColumnDefinition Width="Auto"/>
<ColumnDefinition Width="*"/>
<ColumnDefinition Width="Auto"/>
</Grid.ColumnDefinitions>

<Button Content="+ Новое бронирование" Click="AddBooking_Click" Background="#27AE60" Foreground="White" Padding="15,8" BorderThickness="0" Cursor="Hand">
<Button.Template>
<ControlTemplate TargetType="Button">
<Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
<ContentPresenter/>
</Border>
</ControlTemplate>
</Button.Template>
</Button>

<StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0">
<TextBlock Text="С:" VerticalAlignment="Center" Margin="0,0,5,0"/>
<DatePicker x:Name="FilterStartDatePicker" Width="120" Margin="0,0,10,0"/>
<TextBlock Text="По:" VerticalAlignment="Center" Margin="0,0,5,0"/>
<DatePicker x:Name="FilterEndDatePicker" Width="120" Margin="0,0,15,0"/>
                    
<TextBlock Text="Номер:" VerticalAlignment="Center" Margin="0,0,5,0"/>
<ComboBox x:Name="FilterRoomComboBox" Width="150" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,10,0"/>
                    
<TextBlock Text="Статус:" VerticalAlignment="Center" Margin="0,0,5,0"/>
<ComboBox x:Name="FilterStatusComboBox" Width="130" Margin="0,0,10,0">
<ComboBoxItem Content="Все статусы" Tag="All" IsSelected="True"/>
<ComboBoxItem Content="Ожидание" Tag="Pending"/>
<ComboBoxItem Content="Подтверждено" Tag="Confirmed"/>
<ComboBoxItem Content="Оплачено" Tag="Paid"/>
<ComboBoxItem Content="Отменено" Tag="Cancelled"/>
<ComboBoxItem Content="Завершено" Tag="Completed"/>
</ComboBox>
<Button Content="Сбросить" Click="ClearFilter_Click" Padding="10,5"/>
</StackPanel>
</Grid>
</Border>

<Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15">
<DataGrid x:Name="BookingsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White" BorderThickness="0">
<DataGrid.Columns>
<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
<DataGridTextColumn Header="Номер" Binding="{Binding Room.Name}" Width="100"/>
<DataGridTextColumn Header="Клиент" Binding="{Binding Client.FullName}" Width="150"/>
<DataGridTextColumn Header="Заезд" Binding="{Binding CheckInDate, StringFormat=dd.MM.yyyy}" Width="100"/>
<DataGridTextColumn Header="Выезд" Binding="{Binding CheckOutDate, StringFormat=dd.MM.yyyy}" Width="100"/>
<DataGridTextColumn Header="Дней" Binding="{Binding Days}" Width="60"/>
<DataGridTextColumn Header="Сумма" Binding="{Binding TotalPrice, StringFormat={}{0:N0}}" Width="100"/>
<DataGridTextColumn Header="Оплачено" Binding="{Binding PaidAmount, StringFormat={}{0:N0}}" Width="100"/>
<DataGridTemplateColumn Header="Статус" Width="110">
<DataGridTemplateColumn.CellTemplate>
<DataTemplate>
<Border CornerRadius="10" Padding="8,4">
<Border.Style>
<Style TargetType="Border">
<Style.Triggers>
<DataTrigger Binding="{Binding Status}" Value="Pending"><Setter Property="Background" Value="#F39C12"/></DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Confirmed"><Setter Property="Background" Value="#3498DB"/></DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Paid"><Setter Property="Background" Value="#27AE60"/></DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="CheckedIn"><Setter Property="Background" Value="#9B59B6"/></DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Cancelled"><Setter Property="Background" Value="#E74C3C"/></DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Completed"><Setter Property="Background" Value="#95A5A6"/></DataTrigger>
</Style.Triggers>
</Style>
</Border.Style>
<TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" FontSize="11"/>
</Border>
</DataTemplate>
</DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>
<DataGridTemplateColumn Header="Действия" Width="*">
<DataGridTemplateColumn.CellTemplate>
<DataTemplate>
<StackPanel Orientation="Horizontal">
<!-- Кнопка Оплата - показывается если не оплачено полностью и бронь активна -->
<Button Content="Опл." Click="PayBooking_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Padding="5,2" Margin="2,0" ToolTip="Оплатить">
<Button.Style>
<Style TargetType="Button">
<Setter Property="Visibility" Value="Visible"/>
<Style.Triggers>
<DataTrigger Binding="{Binding Status}" Value="Cancelled">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Completed">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
<DataTrigger Binding="{Binding IsFullyPaid}" Value="True">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
</Style.Triggers>
</Style>
</Button.Style>
</Button>
<!-- Кнопка Редактировать -->
<Button Content="Ред." Click="EditBooking_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Padding="5,2" Margin="2,0" ToolTip="Редактировать">
<Button.Style>
<Style TargetType="Button">
<Setter Property="Visibility" Value="Visible"/>
<Style.Triggers>
<DataTrigger Binding="{Binding Status}" Value="Cancelled">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Completed">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
</Style.Triggers>
</Style>
</Button.Style>
</Button>
<!-- Кнопка Заезд - показывается если бронь подтверждена или оплачена -->
<Button Content="Заезд" Click="CheckIn_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Padding="5,2" Margin="2,0" ToolTip="Заселить">
<Button.Style>
<Style TargetType="Button">
<Setter Property="Visibility" Value="Collapsed"/>
<Style.Triggers>
<DataTrigger Binding="{Binding Status}" Value="Confirmed">
<Setter Property="Visibility" Value="Visible"/>
</DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Paid">
<Setter Property="Visibility" Value="Visible"/>
</DataTrigger>
</Style.Triggers>
</Style>
</Button.Style>
</Button>
<!-- Кнопка Выезд - показывается если гость заселён -->
<Button Content="Выезд" Click="CheckOut_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Padding="5,2" Margin="2,0" ToolTip="Выселить">
<Button.Style>
<Style TargetType="Button">
<Setter Property="Visibility" Value="Collapsed"/>
<Style.Triggers>
<DataTrigger Binding="{Binding Status}" Value="CheckedIn">
<Setter Property="Visibility" Value="Visible"/>
</DataTrigger>
</Style.Triggers>
</Style>
</Button.Style>
</Button>
<!-- Кнопка Отмена - показывается если бронь активна -->
<Button Content="Отм." Click="CancelBooking_Click" Tag="{Binding}" Background="Transparent" BorderThickness="0" Padding="5,2" Margin="2,0" ToolTip="Отменить" Foreground="Red">
<Button.Style>
<Style TargetType="Button">
<Setter Property="Visibility" Value="Visible"/>
<Style.Triggers>
<DataTrigger Binding="{Binding Status}" Value="Cancelled">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="Completed">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
<DataTrigger Binding="{Binding Status}" Value="CheckedIn">
<Setter Property="Visibility" Value="Collapsed"/>
</DataTrigger>
</Style.Triggers>
</Style>
</Button.Style>
</Button>
</StackPanel>
</DataTemplate>
</DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>
</DataGrid.Columns>
</DataGrid>
</Border>
</Grid>
</Page>
