<Window x:Class="HotelSystem.Views.ServicePaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оплата услуги" Height="450" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F6FA">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="💳 Оплата дополнительной услуги" FontWeight="Bold" FontSize="16" Margin="0,0,0,20" Foreground="#2C3E50"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Клиент (бронирование):" Margin="0,0,0,5" FontSize="14"/>
            <ComboBox x:Name="BookingComboBox" DisplayMemberPath="DisplayInfo" SelectionChanged="BookingComboBox_SelectionChanged" Padding="10" FontSize="14" Height="35"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Услуга:" Margin="0,0,0,5" FontSize="14"/>
            <ComboBox x:Name="ServiceComboBox" DisplayMemberPath="Name" SelectionChanged="ServiceComboBox_SelectionChanged" Padding="10" FontSize="14" Height="35"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Количество:" Margin="0,0,0,5" FontSize="14"/>
            <TextBox x:Name="QuantityTextBox" Text="1" Padding="10" TextChanged="QuantityTextBox_TextChanged" FontSize="16" TextAlignment="Right"/>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Итого:" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="TotalText" FontSize="20" FontWeight="Bold" Foreground="#27AE60"/>
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="Cancel_Click" Padding="20,10" Margin="0,0,10,0"/>
            <Button Content="Оплатить" Click="Pay_Click" Background="#27AE60" Foreground="White" Padding="20,10" BorderThickness="0">
                <Button.Template><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center"/></Border></ControlTemplate></Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
