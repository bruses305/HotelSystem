<Window x:Class="HotelSystem.Views.PaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оплата бронирования" Height="350" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F5F6FA">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="💳 Оплата бронирования" FontWeight="Bold" FontSize="16" Margin="0,0,0,20" Foreground="#2C3E50"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="К оплате:" FontSize="14"/>
            <TextBlock Grid.Column="1" x:Name="TotalText" FontWeight="Bold" FontSize="14"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Оплачено:" Foreground="#7F8C8D" FontSize="14"/>
            <TextBlock Grid.Column="1" x:Name="PaidText" Foreground="#7F8C8D" FontSize="14"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Осталось:" Foreground="#E74C3C" FontSize="14" FontWeight="SemiBold"/>
            <TextBlock Grid.Column="1" x:Name="RemainingText" Foreground="#E74C3C" FontSize="14" FontWeight="SemiBold"/>
        </Grid>

        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Сумма платежа:" Margin="0,0,0,5" FontSize="14"/>
            <TextBox x:Name="AmountTextBox" Padding="10" FontSize="16" TextChanged="AmountTextBox_TextChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="Cancel_Click" Padding="20,10" Margin="0,0,10,0"/>
            <Button Content="Оплатить" Click="Pay_Click" Background="#27AE60" Foreground="White" Padding="20,10" BorderThickness="0">
                <Button.Template><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}"><ContentPresenter HorizontalAlignment="Center"/></Border></ControlTemplate></Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
