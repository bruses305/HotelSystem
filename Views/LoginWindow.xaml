<Window x:Class="HotelSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HotelSystem.Views"
        Title="Hotel System - Вход" 
        Height="640" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        Language="ru-RU">

    <Window.Resources >
        <local:TextToVisibilityConverter x:Key="TextToVisibilityConverter"/>

        <Color x:Key="DarkBgColor">#FF1A2530</Color>
        <Color x:Key="AccentColor">#FF2C6C9E</Color>
        <Color x:Key="AccentHoverColor">#FF1F4D73</Color>
        <Color x:Key="TextSecondaryColor">#FF6B7A8A</Color>
        <Color x:Key="ErrorColor">#FFD32F2F</Color>

        <SolidColorBrush x:Key="DarkBgBrush" Color="{StaticResource DarkBgColor}"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

        <!-- Анимация появления -->
        <Storyboard x:Key="WindowEnterAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                             From="0.95" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                             From="0.95" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Стиль текстового поля: текст ЧЁРНЫЙ -->
        <Style x:Key="FloatingHintTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,10,10,10"/>
            <Setter Property="BorderBrush" Value="#FFD0D5DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF000000"/> <!-- Чёрный текст -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Margin="{TemplateBinding Padding}"
                                              Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <TextBlock x:Name="hint" 
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="14,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource TextToVisibilityConverter}}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для PasswordBox: текст ЧЁРНЫЙ -->
        <Style x:Key="FloatingHintPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,10,10,10"/>
            <Setter Property="BorderBrush" Value="#FFD0D5DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF000000"/> <!-- Чёрный текст -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Margin="{TemplateBinding Padding}"
                                              Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <TextBlock x:Name="hint" 
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="14,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="{Binding Password, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource TextToVisibilityConverter}}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Анимация тряски -->
        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0:0:0.00" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-5"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.10" Value="5"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-5"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.20" Value="5"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Window.Resources>

    <Window.RenderTransform>
        <ScaleTransform/>
    </Window.RenderTransform>

    <Grid MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Grid.Background>
            <SolidColorBrush Color="{StaticResource DarkBgColor}"/>
        </Grid.Background>

        <Border x:Name="LoginCard"
                Background="{StaticResource CardBgBrush}"
                CornerRadius="20"
                Margin="40"
                Padding="30,40"
                VerticalAlignment="Center"
                MaxWidth="400">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" Opacity="0.15" ShadowDepth="0" Color="Black"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="🏨" FontSize="48" HorizontalAlignment="Center" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>
                <TextBlock Text="Гостиничная система" FontSize="28" FontWeight="SemiBold" Foreground="#FF2C3E50" HorizontalAlignment="Center" Margin="0,8,0,4"/>
                <TextBlock Text="Вход в систему" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,28"/>

                <TextBox x:Name="LoginTextBox" 
                         Tag="Логин или email"
                         Style="{StaticResource FloatingHintTextBox}"
                         Foreground="#FF000000"
                         FontSize="16"
                         Margin="0,6,0,16"/>

                <Grid Margin="0,6,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <PasswordBox x:Name="PasswordBox" 
                                 Tag="Пароль"
                                 Style="{StaticResource FloatingHintPasswordBox}"
                                 Grid.Column="0"
                                 Visibility="Visible"
                                 PasswordChanged="PasswordBox_PasswordChanged"/>
                    <TextBox x:Name="VisiblePasswordTextBox"
                             Tag="Пароль"
                             Style="{StaticResource FloatingHintTextBox}"
                             Grid.Column="0"
                             Visibility="Collapsed"/>
                    <ToggleButton x:Name="TogglePasswordVisibility"
                                  Grid.Column="1"
                                  Width="40" Height="40"
                                  Margin="8,0,0,0"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  Cursor="Hand"
                                  VerticalAlignment="Center"
                                  IsChecked="False">
                        <ToggleButton.Style>
                            <Style TargetType="ToggleButton">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <TextBlock Text="🙈" FontSize="18" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <TextBlock Text="👁" FontSize="18" Foreground="{StaticResource AccentBrush}"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ToggleButton.Style>
                    </ToggleButton>
                </Grid>

                <Grid Margin="0,0,0,28">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox x:Name="RememberMeCheckBox" 
                              Content="Запомнить меня"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              FontSize="12"
                              Margin="0,0,16,0"/>
                </Grid>

                <Button x:Name="LoginButton"
                        Content="ВОЙТИ"
                        Click="LoginButton_Click"
                        Cursor="Hand"
                        IsDefault="True"
                        Height="46"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Background="{StaticResource AccentBrush}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="{StaticResource AccentColor}" Opacity="0.3"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <ProgressBar x:Name="LoginProgressBar"
                             IsIndeterminate="True"
                             Foreground="{StaticResource AccentBrush}"
                             Background="#E8ECF0"
                             Height="3"
                             Margin="0,20,0,0"
                             Visibility="Collapsed"/>

                <Border x:Name="ErrorBorder" 
                        Background="#FFF5E6E6" 
                        CornerRadius="8" 
                        Margin="0,20,0,0"
                        Padding="12,10"
                        BorderBrush="#FFE0B3B3"
                        BorderThickness="1"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" FontSize="12" Foreground="{StaticResource ErrorBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="ErrorTextBlock" 
                                   Foreground="{StaticResource ErrorBrush}" 
                                   TextWrapping="Wrap"
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
        <!-- Индикатор раскладки -->
        <Border Grid.Row="0" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="0,20,30,0"
                Background="#CCFFFFFF" 
                CornerRadius="12" 
                Padding="10,5"
                BorderBrush="{StaticResource AccentBrush}"
                BorderThickness="1">
            <TextBlock x:Name="LanguageIndicator" 
                       Text="??" 
                       FontSize="14" 
                       FontWeight="SemiBold" 
                       Foreground="{StaticResource AccentBrush}"
                       TextAlignment="Center"/>
        </Border>
    </Grid>
</Window>