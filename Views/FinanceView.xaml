<Page x:Class="HotelSystem.Views.FinanceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#ECF0F1">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Background="#27AE60" CornerRadius="8" Padding="20" Margin="0,0,7,0">
                <StackPanel><TextBlock Text="Доходы" Foreground="White" FontSize="14"/><TextBlock x:Name="IncomeText" Text="0" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,10,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="1" Background="#E74C3C" CornerRadius="8" Padding="20" Margin="7,0">
                <StackPanel><TextBlock Text="Расходы" Foreground="White" FontSize="14"/><TextBlock x:Name="ExpensesText" Text="0" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,10,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Background="#3498DB" CornerRadius="8" Padding="20" Margin="7,0,0,0">
                <StackPanel><TextBlock Text="Прибыль" Foreground="White" FontSize="14"/><TextBlock x:Name="ProfitText" Text="0" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,10,0,0"/></StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="StartDatePicker" Width="120" Margin="0,0,10,0"/>
                <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="EndDatePicker" Width="120" Margin="0,0,10,0"/>
                <Button Content="Применить" Click="ApplyFilter_Click" Padding="15,5"/>
                <Button Content="+ Добавить транзакцию" Click="AddTransaction_Click" Margin="20,0,0,0" Background="#27AE60" Foreground="White" Padding="15,5" BorderThickness="0">
                    <Button.Template><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border></ControlTemplate></Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15">
            <DataGrid x:Name="TransactionsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="White" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding TransactionDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTemplateColumn Header="Тип" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="8,4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="Income"><Setter Property="Background" Value="#27AE60"/></DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="Expense"><Setter Property="Background" Value="#E74C3C"/></DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Type}" Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:N0}}" Width="120"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
